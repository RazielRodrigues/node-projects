const sinon = require('sinon');
const { expect } = require('chai');

describe("database" , () => {

    beforeEach(() => {
        sandbox = new sinon.sandbox();
    });

    afterEach(() => {
        sandbox.restore()
    });


    it("should return a promise", () => {
        expect(value).to.;
    })
})